/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://markcoder.tech',
  generateRobotsTxt: true, // Generate robots.txt
  changefreq: 'daily', // Change frequency of pages
  priority: 0.7, // Default priority of URLs
  sitemapSize: 5000,
};
